# URL de base de l'API backend (Django REST Framework).
# Laisser vide pour utiliser le proxy relatif par défaut : /api/v1
# En développement local contre un backend séparé, par ex. :
# VITE_API_URL=http://localhost:8000/api/v1
VITE_API_URL=
